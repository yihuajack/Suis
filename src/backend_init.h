#include <filesystem>

#include "Config.h"

namespace Suis {
    const std::filesystem::path SUIS_ROOT = "..";
    const std::filesystem::path default_config = SUIS_ROOT / "suis.ini";
    // SuisConfig config;
}
