//
// Created by Yihua Liu on 2024-7-4.
//

#include "OpticStack.h"

template<typename T>
OpticStack<T>::OpticStack(const std::vector<std::pair<std::string, double>> &structure, bool no_back_reflection = false,
                          const std::string &substrate = "", const std::string &incidence = "") {
    for (const auto &[name, thickness] : structure) {

    }
}
